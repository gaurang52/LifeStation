module.exports = {
  // react-native-vector-icons pod handles fonts automatically
  // No need to link fonts manually as it causes duplicate build errors
  // For Open Sans (tagline): add OpenSans-Regular.ttf to src/assets/fonts — see that folder's README
};
